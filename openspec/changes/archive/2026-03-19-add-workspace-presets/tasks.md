## 1. 工作区预设持久化

- [x] 1.1 在 `bin/kickstart.js` 中新增 `workspaces.json` 路径常量及对应的 JSON 读写方法
- [x] 1.2 实现预设的保存、读取、重命名、删除方法，并补上名称唯一性校验与 `updatedAt` 更新时间维护
- [x] 1.3 沿用现有文件读取容错方式，在预设文件不存在或内容异常时回退为空列表

## 2. 启动与打开流程

- [x] 2.1 新增启动首页选择，让用户在“最近项目”“工作区预设”“管理预设”之间切换
- [x] 2.2 改造最近项目流程，在勾选完成后提供“直接打开”和“保存为预设后打开”两个动作
- [x] 2.3 新增预设打开流程，展示预设列表、校验仓库路径、提示被跳过的失效路径，并打开剩余有效仓库

## 3. 预设管理交互

- [x] 3.1 新增预设管理入口，并在没有任何预设时给出明确提示
- [x] 3.2 实现预设重命名流程，包含重名校验与持久化更新
- [x] 3.3 实现预设删除流程，包含确认步骤与持久化更新

## 4. 验证与补充

- [ ] 4.1 手动验证最近项目打开、保存为预设、从预设打开、重命名、删除等核心流程
- [ ] 4.2 确认新增分流后，`kickstart` 仍能正常在 iTerm2 中打开所选仓库
- [x] 4.3 更新 `README.md`，补充工作区预设能力和新的启动入口说明
